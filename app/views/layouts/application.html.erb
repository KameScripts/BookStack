<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "ReadNavi" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <div class="min-h-screen flex flex-col">
      <%# ヘッダー：右端にログイン/ログアウトを配置 %>
      <header class="h-16 text-white bg-gray-700 sticky top-0 z-50 w-full px-8 flex justify-between items-center">
        <div class="text-3xl font-bold">ReadNavi</div>
        <div class="text-base font-normal">
          <% if logged_in? %>
            <%= link_to "ログアウト", logout_path, data: { turbo_method: :delete }, class: "hover:underline" %>
          <% else %>
            <%= link_to "ログイン", login_path, class: "hover:underline" %>
          <% end %>
        </div>
      </header>

      <div class="flex flex-1">
        <nav id="sidebarMenu" class="w-36 bg-gray-400 text-white sticky top-16 h-[calc(100vh-4rem)]">
          <ul>

              <li class="px-4 pt-5 transition hover:text-neutral-600">
                <%= link_to "本一覧", books_path %>
              </li>
              <li class="px-4 py-2.5 transition hover:text-neutral-600">
                <%= link_to "新規登録", new_book_path %>
              </li>

          </ul>
        </nav>

        <main class="flex-1 p-8">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>